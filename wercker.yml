box:
    id: golang
    ports:
        - "5000"

dev:

    steps:
        - internal/watch:
            code: |
                go build ./...
                ./source
            reload: true

# Build definition
build:
    steps:

    # golint step!
    - wercker/golint

    # Build the project
    - script:
        name: go build
        code: |
            go build ./...

    - script:
        name: hello-world
        echo: WERCKER_ROOT=$WERCKER_ROOT
        code: ls $WERCKER_ROOT
        echo: WERCKER_SOURCE_DIR=$WERCKER_SOURCE_DIR
        code: ls $WERCKER_SOURCE_DIR
        
    # Test the project
    - script:
        name: go test
        code: |
            go test ./...

    - internal/docker-push:
        username: $DOCKERHUB_USERNAME
        password: $DOCKERHUB_PASSWORD
        cmd: /main
        tag: latest
        repository: rikgibson/fun-with-golang

push-docker:
    steps:
        - internal/docker-push:
            username: $DOCKERHUB_USERNAME
            password: $DOCKERHUB_PASSWORD
            cmd: /main
            tag: latest
            repository: rikgibson/fun-with-golang

hello-world:
    steps:

    - script:
        name: hello-world
        code: echo $HELLO_MESSAGE